---
title: "ADAMANT v0.8.0"
slug: "release-adamant-v0-8-0-88981571"
description: "このリリースではconfig.jsonにcorsオプションが追加され、/api/transactionsおよび/api/chats/getエンドポイントでinIdまたはisInが使用可能になりました。たとえば、/api/chats/get?InId=U6386412615727665758など。"
category: "release"
source: "github"
sourceUrl: "https://github.com/Adamant-im/adamant/releases/tag/v0.8.0"
publishedAt: "2023-01-14T18:15:03Z"
author: "martiliones"
authorUrl: "https://github.com/martiliones"
repo: "adamant"
tag: "v0.8.0"
prerelease: false
cardSpan: "half"
originalId: "github-release:adamant:88981571"
locale: "ja"
placeholder: false
---

このADAMANTのリリースでは、`config.json` に `cors` オプションが導入されました。ユーザーは、`/api/transactions` および `/api/chats/get` エンドポイントで、`inId` または `isIn` のいずれかを使用できるようになりました。たとえば、`/api/chats/get?InId=U6386412615727665758` と `/api/chats/get?isIn=U6386412615727665758` は同等です。

`/api/chats/get` および `/api/chatrooms` エンドポイントにおけるユーザーIDは、現在大文字小文字を区別しません。たとえば、`/api/chatrooms/U6386412615727665758` と `/api/chatrooms/u6386412615727665758` は同じです。

トランザクションおよび `POST /api/accounts/delegates` は、トランザクションをプレーンオブジェクトとして、または `transaction` プロパティ内に含めて受け付けるようになりました。

```bash
curl -X POST https://endless.adamant.im/api/transactions/process
   -H 'Content-Type: application/json'
   -d '{"type": 0, "amount": 100000000, ...}'  # or {"transaction": { "type": 0, "amount": 100000000, ... }}
```

`/api/states/get` エンドポイントには、現在 `confirmations` プロパティが含まれます。`generatorPublicKey` クエリの最適化が適用されました。「permission denied for schema public」エラーが修正され、その他いくつかの軽微な変更も含まれています。

### Breaking changes

`/api/blocks` エンドポイントは、`count` プロパティを返さなくなりました。
