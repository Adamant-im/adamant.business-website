---
title: "ADAMANT v0.8.0"
slug: "release-adamant-v0-8-0-88981571"
description: "Esta versión de ADAMANT introduce una opción cors en config.json. Los usuarios pueden usar inId o isIn en los endpoints /api/transactions y /api/chats/get."
category: "release"
source: "github"
sourceUrl: "https://github.com/Adamant-im/adamant/releases/tag/v0.8.0"
publishedAt: "2023-01-14T18:15:03Z"
author: "martiliones"
authorUrl: "https://github.com/martiliones"
repo: "adamant"
tag: "v0.8.0"
prerelease: false
cardSpan: "half"
originalId: "github-release:adamant:88981571"
locale: "es"
placeholder: false
---

Esta versión de ADAMANT introduce una opción `cors` en `config.json`. Ahora los usuarios pueden usar `inId` o `isIn` en los endpoints `/api/transactions` y `/api/chats/get`. Por ejemplo, `/api/chats/get?InId=U6386412615727665758` y `/api/chats/get?isIn=U6386412615727665758` son equivalentes.

Los ID de usuario en los endpoints `/api/chats/get` y `/api/chatrooms` ahora son insensibles a mayúsculas y minúsculas. Por ejemplo, `/api/chatrooms/U6386412615727665758` y `/api/chatrooms/u6386412615727665758` son iguales.

Las transacciones y `POST /api/accounts/delegates` ahora aceptan una transacción como un objeto plano o dentro de la propiedad `transaction`.

```bash
curl -X POST https://endless.adamant.im/api/transactions/process
   -H 'Content-Type: application/json'
   -d '{"type": 0, "amount": 100000000, ...}'  # or {"transaction": { "type": 0, "amount": 100000000, ... }}
```

El endpoint `/api/states/get` ahora incluye una propiedad `confirmations`. Se ha aplicado una optimización de consulta en `generatorPublicKey`. Se ha corregido un error de "permiso denegado para el esquema público", junto con otros cambios menores.

### Cambios importantes

El endpoint `/api/blocks` ya no devuelve la propiedad `count`.
