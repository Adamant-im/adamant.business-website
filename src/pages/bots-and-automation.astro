---
import { getEntry } from 'astro:content';
import ServicePage from '../components/ServicePage.astro';

const entry = await getEntry('services', 'en/bots-and-automation');
if (!entry) throw new Error('Service content not found');
---

<ServicePage entry={entry} locale="en" />
